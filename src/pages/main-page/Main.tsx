import MainCarousel from "../../components/main-carousel/MainCarousel";
import { MainWrapper } from "./Main.Styled";

const Main = () => {
  return (
    <MainWrapper>
      <MainCarousel />
    </MainWrapper>
  );
};

export default Main;
